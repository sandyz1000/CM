define([
//    "jquery",
        "underscore",
        "backbone",
    ],
    function( _, Backbone) {

        var app = {};

        //Event handlers for event_target and event_id etc.
        //  $('.event').on('click', function (event) {
        //       console.log("Captured a change event");
        //       console.log(event);
        // });
        return app;
    });